extern crate rand;
extern crate rusty_machine as rm;

use rusty_machine::prelude::*;

const visible_distance: u8 = 4;
const input_dim: u8 = 4 * (2 * visible_distance + 1) * (2 * visible_distance + 1);


fn main() {
  println!("Hello, world!");
}
